<resources>

    <!-- Base application theme. -->
    <style name="Base.Theme.App" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@null</item>
        <item name="textAllCaps">false</item>
        <!-- Customize your light theme here. -->
        <!-- colorPrimary: 应用的主要色调, ActionBar 默认使用该颜色,Toolbar导航栏的底色 -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <!-- colorControlNormal: CheckBox, RadioButton, SwitchCompat, ProgressBar 等默认状态的颜色 -->
        <item name="colorControlNormal">@color/colorControlNormal</item>
        <!-- colorAccent: CheckBox, RadioButton, SwitchCompat, ProgressBar 等控件的选中效果默认采用该颜色; EditTex获取焦点的时候的下划线和光标颜色 -->
        <item name="colorAccent">@color/colorAccent</item>
        <!-- colorControlHighlight: 控件按压时的色调 -->
        <item name="colorControlHighlight">@color/colorControlHighlight</item>
    </style>

    <style name="Theme.App" parent="Base.Theme.App" />

    <!-- Only for xml layout preview -->
    <style name="Xml.Preview" parent="Theme.App">
        <item name="android:windowBackground">@color/bg_primary</item>
    </style>

    <style name="Base.Dialog" parent="Theme.Material3.DayNight.Dialog">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:backgroundDimAmount">0.4</item>
    </style>

    <style name="Base.Dialog.Common">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:windowAnimationStyle">@style/CenterDialogAnimation</item>
    </style>

    <style name="Base.Dialog.Common.BottomSheet">
        <item name="android:windowAnimationStyle">@style/Animation.Design.BottomSheetDialog</item>
    </style>

    <style name="Base.Dialog.Common.FullScreen">
        <item name="android:backgroundDimEnabled">false</item>
    </style>

    <style name="CenterDialogAnimation">
        <item name="android:windowEnterAnimation">@anim/fade_in_center</item>
        <item name="android:windowExitAnimation">@anim/fade_out_center</item>
    </style>

    <style name="HomePopAnimation">
        <item name="android:windowEnterAnimation">@anim/fade_in_top</item>
        <item name="android:windowExitAnimation">@anim/fade_out_top</item>
    </style>

</resources>