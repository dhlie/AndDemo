apply plugin: 'java'

dependencies {
    implementation fileTree(dir: 'libs', include: ['*.jar'])
}

sourceCompatibility = build_versions.javaSourceCompatibility
targetCompatibility = build_versions.javaTargetCompatibility
